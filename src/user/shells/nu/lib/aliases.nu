alias x = dexit
alias xa = exit

alias la = ls -a
alias ll = ls -al

alias md = mkdir

alias sd = sudo

alias mx = chmod +x
alias smx = sudo chmod +x
alias own = chown -R $"($env.USER):($env.USER)"
alias sown = sudo chown -R $"($env.USER):($env.USER)"

alias cs = clear

alias h = code .
alias H = code-insiders .
